I want to create a work out template for 531 BBB.

It should be clean and a minimal mvp, we can build it strait in the @app/page.tsx file (breaking out and components as we need). First, let's get an input that accepts our training maxes (90% of a 1 rep max).

There should be 4 inputs (
